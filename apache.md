# To install apache:
sudo apt install apache2
sudo apt install w3m

Result: installs the Apache web server and the language interpreter. 

# If the default webpage is working properly, it will say 'It Works' at the bottom.

# Daniel's VM public IP 34.127.173.177

# http://34.127.173.177/index.html took you to the webpage. 
