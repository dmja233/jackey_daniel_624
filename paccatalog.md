#This module has been fairly difficult. 

#UN: libcat
#PSW: Wildcats!5**!

#Issue: Not able to log into the webpage for testing. Solve: I forgot to update the .htaccess page. 

Change group ownership:
 sudo chown :www-data /var/www/html

 All further files change ownership:
  sudo find /var/www/html -type d -exec chmod g+s {} +

  SELECT, WHERE, ORDER BY, JOIN, and GROUP BY are all functions that can be used in a SQL database command line. 

  Select chooses
  Where defines
  Order by...orders by
  Join connects
  Group by...groups by
